# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'KBanktTestProject' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for KBanktTestProject
  pod 'PureLayout', "~> 3.1.6"
  pod "PromiseKit", "~> 6.8"
  pod 'Alamofire', '~> 5.2'
  pod 'SwiftyJSON', '~> 4.0'

  target 'KBanktTestProjectTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
